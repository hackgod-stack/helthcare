[package]
name = "differential_privacy"
version = "0.1.0"
edition = "2021"

[dependencies]
serde.workspace = true
rand.workspace = true
sha2.workspace = true

# Statistical distributions for noise generation
statrs = "0.16"